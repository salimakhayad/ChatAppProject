@model ChatApp.Models.Home.HomeModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center" id="profielen">
    <h1 class="display-4">Profielen</h1>
    
</div>
@{int rowCount = Model.Profielen.Count() / 3;

    int profileCounter = @Model.Profielen.Count();


     <div id="profielen" class="row">
        @foreach (var profile in Model.Profielen)
        {
            <div class="col-lg-4">
                <svg class="bd-placeholder-img rounded-circle" width="200" height="100" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: 200x100"><title>Placeholder</title><rect width="100%" height="100%" fill="#777"></rect><text x="50%" y="50%" fill="#777" dy=".3em">140x140</text></svg>
                <h2>@profile.UserName</h2>
                <p>@profile.Naam</p>
                <p><a class="btn btn-secondary" href="#" role="button">Stuur berichtje »</a></p>
            </div><!-- /.col-lg-4 -->
        }
     </div>
}
